<% @user ||= current_user %>
<div class="stats">
    <a href="<%= following_user_path(@user) %>">
        <strong id="following" class="stat">
            <%= @user.followed_users.count %>
        </strong>
        Following
    </a>
    <a href="<%= followers_user_path(@user) %>">
        <strong id="followers" class="stat">
            <%= @user.followers.count %>
        </strong>
        Followers 
    </a>
      <a href="<%= bookmarkers_user_path(@user) %>">
        <strong id="bookmarkers" class="stat">
             <%= @user.bookmarkers.count %>
        </strong>
       Recommended 
    </a>
   
</div>