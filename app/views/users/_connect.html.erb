<%= form_for(current_user.relationships.build(followed_id: user.id),
             remote: true) do |f| %>
<div><%= f.hidden_field :followed_id %></div>
<%= button_tag do %>
<div id="add" class="tips icon-btn-add" title="Follow" data-placement="bottom"><a href="#" class="glyphicons white user_add"><i></i></a></div>
<% end %>
<% end %>